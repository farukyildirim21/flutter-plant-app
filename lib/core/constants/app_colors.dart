import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF28AF6E);

  // Text
  static const Color mainText = Color(0xFF13231B);
  static const Color white = Colors.white;

  // Status Bar
  static const Color statusBarIcon = Color(0xFFADB4BB);

  // Background
  static const Color backgroundStart = Color(0xFFF8FAFF);
  static const Color backgroundEnd = Color(0xFFFAFAFA);
  static const Color searchBar = Color(0xE0FFFFFF);
  static const Color tabBar = Color(0xEBFFFFFF);
  static const Color premiumBox = Color(0xFF24201A);

  // Onboarding
  static const Color dotActive = Color(0xFF13231B);
  static const Color dotInactive = Color(0x4013231B);

  // Paywall
  static const Color paywallCard = Color(0x0DFFFFFF);
  static const Color paywallCardSelected = Color(0xFF28AF6E);
  static const Color featureCard = Color(0x14FFFFFF);

  // Borders
  static const Color tabBarBorder = Color(0x1A13231B);
  static const Color paywallBorder = Color(0x4DFFFFFF);

  // Disclaimer
  static const Color disclaimerText = Color(0xB3597165);
  static const Color disclaimerLink = Color(0x85FFFFFF);

  // Gold gradient
  static const Color goldStart = Color(0xFFE6C990);
  static const Color goldEnd = Color(0xFFE4B046);
}
